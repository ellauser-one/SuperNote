# api/src/dto/
> L3 | 父级: ../../CLAUDE.md

成员清单
profile.dto.ts: patchProfileBodySchema / PatchProfileBody / FORBIDDEN_PROFILE_ID_KEYS
memo.dto.ts: createFolder/createMemo/updateMemo/renameNode/moveNode schemas · FORBIDDEN_MEMO_ID_KEYS

## 边界
- 只放 zod schema 与 z.infer
- 禁止 import Hono

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
