# 2026-07-22 — SuperNote 发布冻结收束

任务：把 SuperNote（app/chat/api 三服务）从功能集合收束为可发布版本（发布冻结：只修 broken flow、补关键状态、上线配置、最小反馈闭环；不重写架构、不加大功能）。

## 完成
- app：全局 401→回登录(handleUnauthorized)；NotesLayout 错误态+重试；NewMemoPage 保存断流修复；MemoTree 客户端搜索/筛选；MemoEditorView「AI 自动分类」按钮(POST /agent/memos/classify)；FeedbackDialog + 右下角浮动入口(POST /api/feedback)；AppShell 窄屏响应式兜底 + ToastViewport。新增 shared/ui/toast.tsx、shared/services/unauthorized.ts、widgets/FeedbackDialog。
- api：新增 POST /api/feedback(仅 Supabase REST)、POST /agent/memos/classify(owner 校验→转发 JWT 给 chat /v1/classify→落库 memos.category)；CORS 区分 dev/prod（非 production 通配，prod 严格 CORS_ORIGINS 白名单）；env 加 CHAT_BASE_URL。
- chat：新增 GET /health、POST /v1/classify（复用模型配置，返回 {category}，失败 502 不泄 key）。
- DB：scripts/sql/round02_feedback.sql（public.feedback 建表 + memos.category 列 + RLS）。**未执行**（Supabase MCP 未连接）。
- 文档：docs/demo-script.md、docs/launch-checklist.md、docs/CLAUDE.md、scripts/sql/CLAUDE.md；更新 api/CLAUDE.md、app/CLAUDE.md（feedback/classify 模块）。

## 验收
- app `bun run build` 通过；api/chat `tsc --noEmit` 通过(0)；grep dist 无 service_role。
- 运行时 health / feedback 提交需 Supabase 在线 + 执行 round02 迁移后验证（本环境未连 Supabase，未跑运行时）。

## 遗留（上线前/下一轮）
- 根 `.env.local` 的 SUPABASE_URL 疑似错拼（JWT 拼到 URL 后），需拆成独立 SUPABASE_SERVICE_ROLE_KEY 行。
- api/.env.example 含占位 service_role，确认非真实 key。
- 核心路径 #5/#6 为最小闭环：分类仅存 memos.category 文本，未做文件夹自动建夹/树重排；搜索仅客户端过滤已加载节点。

## UI 审计与精进修复（备忘录 + AI 对话框）

- 设计系统基础设施：Dialog 新增 `asChild` 支持；Button 新增 `flat` 属性
- 新增设计 token：`--layout-session-list`、`--max-preview-content`、`.ds-button--flat`
- 删除重复样式：`.ds-tree-toolbar__btn`、`.ds-memo-save-toast__retry`（替换为 shared/ui 组件）
- 11 个组件文件统一使用 shared/ui 组件替代自定义按钮/输入/弹层
- 文案统一："Memo Tree"→"备忘录"，移除 "Memo Agent" 冗余英文标签
- 所有 loading 态添加 spinner；右键菜单视口边缘自动翻转修复
- vite build 通过，grep 验证无残留 hex/inline style/arbitrary px
- 待优化项整理至项目根目录 `TODO-UI-OPTIMIZATION.md`
