# app/src/app/router/
> L2 | 父级: ../CLAUDE.md

成员清单
index.tsx: React Router 路由装配；`/` LandingPage 公开；`/app` 经 RequireAuth 后挂 NotesLayout（`/app` 索引 + `/app/notes/:noteId`）与 AppDashboard（`/app/dashboard`）

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
